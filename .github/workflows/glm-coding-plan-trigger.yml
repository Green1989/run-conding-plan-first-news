name: GLM Coding Plan Trigger

on:
  schedule:
    - cron: '0 21 * * *'
    - cron: '0 0 * * *'
    - cron: '30 0 * * *'
    - cron: '0 1 * * *'
    - cron: '30 1 * * *'
    - cron: '0 2 * * *'
    - cron: '30 2 * * *'
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Trigger GLM Coding Plan
        env:
          GLM_API_KEY: ${{ secrets.GLM_API_KEY }}
        run: node scripts/trigger-glm-coding-plan.mjs
